/**
 * MessageType is an enum for different types of messages used while program is
 * running.
 * 
 * @author Terhi Salonen
 * @version 1.0
 */
public enum MessageType {
    OPEN_APP, CLOSE_APP, CHOICES, NEW_CHOICE,
    GET_COUNT, GET_WIDTH, GET_YARNTYPE,
}

// End of File.
